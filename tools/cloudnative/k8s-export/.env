# Kubernetes Resource Export Tool - Environment Variables
# This file contains default values for all configurable environment variables

# Export Configuration
# Output directory for exported resources (default: ./k8s-export-YYYY-MM-DD in current directory)
K8S_EXPORT_OUTPUT_DIR=

# kubectl neat Integration
# Whether to use kubectl neat by default for resource cleaning
USE_NEAT_DEFAULT=true

# Operation Settings
# Clean up temporary files after export
TEMP_DIR_CLEANUP=true

# Show progress during export operations
SHOW_PROGRESS=true

# Export Organization
# Group exported resources by resource type in separate folders
GROUP_BY_RESOURCE_TYPE=true

# Resource Filtering
# Exclude Kubernetes default/system resources from export (comma-separated list)
EXCLUDE_DEFAULT_RESOURCES=kube-root-ca.crt,default-token,kubernetes.io/service-account-token

# Resource Types Discovery
# Use dynamic discovery (true) to get all available resources including CRDs, or fixed common list (false)
# Default: false for faster startup with common resource types only
USE_DYNAMIC_RESOURCE_TYPES=false

# Display Settings
# Enable colored output in terminal
COLOR_OUTPUT=true